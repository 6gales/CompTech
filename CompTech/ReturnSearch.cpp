#include "ReturnSearch.h"



std::multimap<size_t, std::string> ReturnSearch::checkWord(const std::string &source)
{
	size_t editDistance = editDistFormula(source.size());
	std::multimap<size_t, std::string> suggestions;

//	std::stack <

	return suggestions;
}
