#pragma once
#include <fstream>
#include <iostream>
#include <string>
#include <vector>
#include <map>
using namespace std;

map<string, vector<string>> create_empty_dictionary(size_t);
void write_alphabet(string, string, size_t);