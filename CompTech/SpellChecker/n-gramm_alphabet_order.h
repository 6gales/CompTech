#pragma once
#include <fstream>
#include <iostream>
#include <string>
#include <vector>
#include <map>

std::map <std::string, std::vector <std::string>> create_empty_dictionary(size_t);
void write_alphabet(std::string, std::string, size_t);